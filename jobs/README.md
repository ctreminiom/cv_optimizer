# jobs/

Place job postings here as Markdown files. Real postings are gitignored — only synthetic samples under `jobs/sample/` ship with the repo.

```
jobs/
├── sample/
│   ├── senior_backend_engineer.md
│   └── product_manager.md
└── <your_postings>.md     # gitignored
```
